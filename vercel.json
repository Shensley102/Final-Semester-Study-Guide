{
  "version": 2,
  "builds": [
    {
      "src": "study_tool.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "study_tool.py"
    }
  ],
  "env": {
    "PYTHONUNBUFFERED": "1"
  }
}
```

---

## 6. **requirements.txt** (root)
```
Flask==3.0.0
werkzeug==3.0.1
jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
gunicorn==21.2.0
```

---

## 7. **.gitattributes** (root)
```
* text=auto
```

---

## Directory Structure
```
Final-Semester-Study-Guide/
├── study_tool.py              (Flask backend - uses 'template' folder)
├── requirements.txt           (Python dependencies)
├── vercel.json               (Vercel configuration)
├── .gitattributes            (Git configuration)
├── static/
│   ├── script.js             (Frontend JavaScript)
│   └── style.css             (Frontend CSS)
└── template/
    └── index.html            (Main HTML)
